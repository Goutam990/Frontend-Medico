// This page has been disabled because the new backend API does not support calendar view features.
export default function CalendarView() {
  return (
    <div className="min-h-screen bg-gray-50 p-8">
      <h1 className="text-3xl font-bold text-gray-900">Calendar View</h1>
      <p className="mt-2 text-gray-600">
        This feature is currently disabled due to backend API limitations.
      </p>
    </div>
  );
}